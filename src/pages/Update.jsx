import UpdateForm from "../components/UpdateForm";

function Update() {
  return (
    <section className="h-full flex justify-center items-center">
      <UpdateForm btn_name={ 'Update'} />
    </section>
  );
}

export default Update;
