import CreateForm from "../components/CreateForm";

function Create() {
  return (
    <section className="h-full flex justify-center items-center">
      <CreateForm btn_name={"Create"} />
    </section>
  );
}

export default Create;
